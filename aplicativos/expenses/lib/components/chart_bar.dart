import 'package:flutter/material.dart';

class ChartBar extends StatelessWidget {
  final String? day;
  final double? value; 

  const ChartBar({super.key, this.day, this.value});

  @override
  Widget build(BuildContext context){
    return Container();
  }
}